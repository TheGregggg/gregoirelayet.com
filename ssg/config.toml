# The URL the site will be built for
base_url = "https://gregoirelayet.com"

author = "Gregoire Layet"

default_language = "fr"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = false

minify_html = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[languages.en]

[extra]
languages_to_text = {"fr"="Français", "en"="English"}

header_links = [{type="section", path="projects/_index.md"}]

footer_links = [{type="section", path="_index.md"}, {type="section", path="projects/_index.md"}, {type="page", path="mentions_legals.md"}]

github_link = "https://github.com/thegregggg"
github_username = "TheGregggg"
email = "contact@gregoirelayet.com"
